# Python build / cache
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local env & secrets
.env
.env.*

# Local archives & one-off patch helpers
*.zip
_patch_*.py

# Simulation OUTPUTS (regenerated at runtime — not needed in deploy).
# NOTE: matches only baseline result files, NOT the model inputs
# wc2026_goalmodel.json / wc2026_calibrator.json, which MUST stay tracked.
wc2026_baseline*.json

# OS junk
.DS_Store
Thumbs.db
